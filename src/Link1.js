import React from 'react'

export default ({switcher, switchTxt}) => <button 
                style={{marginLeft: 50}}
                 onClick={switcher} >
                 {switchTxt} 
            </button>
        